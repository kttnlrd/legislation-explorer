# Bug plan: every open ticket plus unticketed character classes and nightly source items

Written 2026-09-26 against master @ 80efdc173. Plan only: no code, data or tickets were changed while producing it.
Measurements were taken on 2026-09-26 with read-only queries (scratch in /tmp).

## 0. Preconditions and corrections found while measuring

**Queue confirmed.** `curl -s http://localhost:8765/api/issues` returns 172 records: 143 fixed, 16 resolved, 13 open.
The open set is exactly CDN-0099, 0124, 0173, 0191, 0192, 0193, 0196, 0202, 0204, 0205, 0206, 0209, 0210.
The route truncates expected/actual/note to 200 chars (`backend/routes/issues.py:40-54`). Full notes were read
read-only from the `issues` table through `backend.services.tax_case_sql._sql_dict`.

**The working tree is NOT clean** (the brief said it was). `git status` shows uncommitted changes in:
- `backend/fastmcp_server.py` (+133): fixes for 0204/0205/0206 on the get_section path, plus an unrelated `get_case` absolute-URL change (`_abs`)
- `backend/services/data_loader.py` (+273): `load_case_name_index()` and a new ruling-title extractor
- `backend/routes/cases.py` (6): moves `get_title_for_item` onto `load_case_name_index`
- `scripts/build_definitions_index.py` (+141): 0209/0210 work in progress
- `data/quotes.json` (+11): unrelated
The dev server on :8767 runs this diff. The server on :8765 was started 2026-09-23 without `--reload`, so it still
serves HEAD. That is why the tickets reproduce on :8765 and look fixed on :8767. Batch 0 settles this before anything else.

**Measured figures that differ from the brief:**

| Item | Brief | Measured 2026-09-26 |
|---|---|---|
| (a) "тАв" occurrences | 1,370 in section_index.json | 1,332 in section_index.json (875 chapter_title fields, 22 distinct strings) + 38 in MTG tree.json (22 part titles) = 1,370 |
| (b) case_texts entities | 1,039,390 (563k named / 469k numeric) | 585,838 (116,581 named with `;` / 469,257 numeric), 6,169 files, every file affected |
| (c) MTG ligatures | 18,688 | 18,688 across FB00-FB06 in 1,315 files; 14,726 for FB01-FB05 alone. Also 3,107 in insolvency-keays and 264 in section_index.json |
| (d) stranded bold | nz-it-2007 1,148; aml-ctf-2006 54 | 0 and 0 under every definition tried. aml-ctf-2006 instead has 176 bold headings glued onto the end of the previous paragraph (89 files). nz BOM count 1,947 confirmed |
| C11 counts | 152 / 115 / 480 | glyph 111/38 files, midword 78/31, truncated 78/31. Commit 7bfca6ee8 moved 42 header_split + 18 rowwrap out of "truncated". That was a reclassification, not a repair. Zero C11 findings remain in itaa-1997 |

## Conventions this plan relies on (checked against the repo)

- **Rollback tag, block apply, verify pass.**
  - `scripts/apply_full_ingest.py:160-163` tags `apply-full-{stamp}`, is a dry run unless `--commit` (:25), and has a verify phase (:236).
  - `scripts/apply_operator_proposal.py:175-177` tags `operator-apply-rollback-{head}` and applies per block (:15).
  - `scripts/fix_page_rule_artifacts.py` has `--apply --block-size 50` (:176-178) and `--verify` (:150). **Gap:** it creates no tag, and its provenance log is `/tmp/cdn203-apply.json` (:29).
  - Every repair script below follows `fix_page_rule_artifacts.py` plus the tag step, and logs provenance under `docs/provenance/`, not `/tmp`.
- **A detector must be able to fail.** `scripts/final_data_audit.py:73-76` ("A detector that no longer fires must fail the audit"). Phase 4 runs `scripts/test_c[0-9]*_*.py` (:83).
- **A producer may only delete its own rows.** `scripts/embed_legislation.py:345-358`, :406-411, pinned by `scripts/test_embed_stale_scope.py:43-60`.
- **The pre-commit guard scans staged and untracked files.** `scripts/corpus_change_guard.py:190` (staged) and :194 (`ls-files --others`). Bypass: `CORPUS_GUARD_BYPASS=1` (`scripts/pre_commit_corpus_guard.sh:5-8`). **Gap:** the shell hook exits 0 when no section file is staged (`pre_commit_corpus_guard.sh:11`), so untracked files are only scanned alongside a staged one. See item X1.
- **Detector registries.** There are two, and they disagree:
  - `scan_corpus_error_classes.py` `main()` (:760-766)
  - the nightly's hard-coded 15-name list (`scripts/randomised_api_mcp_test.py:342-347`)
  - This disagreement is the root cause of S3. New classes below are numbered C17-C27; none are in use today.

---

## Part A. Open tickets

### CDN-0099 | Search box has no live suggestions while typing
- **Root cause:**
  - Commit 7066299a5 removed the debounced suggest `useEffect`. Suggestions now load only when Search is pressed (`frontend/src/components/SearchPanel.tsx:85-105`, `runSearchWithSuggestions`).
  - `onChange` only calls `setQuery` (:258-264). The dropdown markup (:311+), keyboard navigation (:180-198) and `pickSuggestion` (:208) are all still there.
  - The backend endpoint exists: `GET /api/search/suggest`, `backend/routes/search.py:361-416`, with client `frontend/src/api.ts:105-109`.
  - The full /search page also exists: `App.tsx:541` and :1081. There is no header search box any more (`App.tsx:965`).
  - So this is a product decision to reverse, not missing code.
- **Fix:**
  - Restore the effect from `git show 7066299a5^:frontend/src/components/SearchPanel.tsx`: 2-character minimum, 200-250 ms debounce, `api.suggest(q, 8)`.
  - `api.suggest` takes no `signal`, so the old abort never worked. Guard with a "latest query" ref so a stale response cannot overwrite a newer one.
  - Enter keeps its current behaviour (full results page).
  - Update the stale changelog line at `backend/routes/api.py:205`.
- **Blast radius:** 1 component, about 25 lines. No data change.
- **Verification:**
  - A vitest/RTL test that types "gene", advances timers 250 ms, and asserts one `api.suggest` call and a rendered dropdown. It fails today because no call is made.
  - A second test: an out-of-order response for "ge" must not replace the results for "gene".
- **Dependencies and risk:** needs Harry's product sign-off, because the removal was deliberate. Adds suggest load on every keystroke burst; the endpoint is FTS5 prefix only, so it is cheap.

### CDN-0124 | Corporations Act: the opening words (chapeau) of about 403 sections were deleted at ingest
- **Root cause:**
  - The source is not a PDF. `scripts/ingest_corps_act.py:23` and :227-237 read `/home/harrison/projects/archived/asic-scraper/finreg.db`, which still holds the chapeau text. Examples: s 189 "...provided by others If:", s 259A "A company must not acquire shares ... except:".
  - The "stray heading" fallback at `ingest_corps_act.py:286-303` deletes any first line under 120 characters unless it starts with `(`, `Note:`, `Example:`, `In this Act:`, `Where ` or `If `. The deletion is `body = rest.strip()` at :303.
  - A line such as "If:" has no trailing space, so it does not match `"If "`, and any chapeau not starting with those prefixes is lost.
- **Fix:**
  - In `ingest_corps_act.py` (:286-303), only drop the line when it does NOT end in `:`, `.` or `;` and is followed by a `(1)`-style subsection marker.
  - Re-ingest to an offline staging directory, then diff against the corpus.
  - Apply only files whose diff is a pure prepend of the finreg.db line. Anything else goes to review.
- **Blast radius:**
  - 313 of 4,163 files currently start at (a)/(b).
  - Re-running the logic against finreg.db, 928 sections lose their first line: 367 end in `:` and 36 in `.`, so about 403 lost real text; the other 525 look like real headings.
  - Same re-ingest as E-e (U+2011).
- **Verification:**
  - New detector `C17_missing_chapeau` (or an extension of existing `scan_chapeau`): a corps section whose first body line after the H1 is `(a)`/`(b)`, while finreg.db has a line ending in `:` before it. It fails today with 313 or more.
  - Verify pass: every changed file equals HEAD with exactly one prepended line, and that line is byte-equal to finreg.db.
- **Dependencies and risk:**
  - finreg.db lives under `projects/archived/`. Pin its sha256 in the provenance log.
  - Real data loss, so prioritise it despite the ticket's "low".
  - Do this in the same re-ingest as E-e, so the corps corpus is rewritten once.

### CDN-0173 | nz-it-2007 carries 445 amendment-history sections from endnote copies of amending Acts
- **Root cause:**
  - `pipeline/parse_nz_it.py:193` runs `soup.find_all("div", class_="part")`, which also matches nested parts inside `div.end > div.skeletons > div.skeleton-act` (endnote copies of at least 11 amending Acts, which produce parts 1/2/3) and inside `div.schedule-amendments` / `div.amend` (which produces part 3B, Tax Administration Act 1994 text).
  - All these copies share part ids "1" and "2", so their files overwrite each other.
  - The source `source/nz-it-2007/whole.html` is already the consolidated Act (Parts A-Z).
- **Fix:**
  - In `parse_nz_it.py` near :193, skip any `div.part` that has an ancestor `div.end`, `div.skeletons`, `div.schedule-amendments` or `div.amend`.
  - Re-parse to staging. Delete only the part-1/2/3/3B section directories (the parser's own output), and rebuild `tree.json`.
  - Combine with E-d (the BOM fix at the same parser's `clean_text`).
- **Blast radius:**
  - 445 section files removed (parts 1: 374, 2: 40, 3: 8, 3B: 23) out of 3,295 leaves.
  - 125 of the 132 amendment-style titles go. The 7 that remain are genuine consolidated titles, e.g. CZ 25, IA 10.
  - Also affected: search index rows, graph nodes and embeddings for those 445 ids, each deleted by its own producer.
- **Verification:**
  - New detector `C18_nz_amendment_part`: an nz-it-2007 tree part id that is not a letter A-Z, or a section title matching `^(Section .* (replaced|repealed)|New section)`. It fails today with 445.
  - Verify pass: every surviving A-Z section is byte-identical apart from the E-d BOM removal.
- **Dependencies and risk:**
  - Deleting 445 files is exactly the kind of change the corpus guard exists for. It needs `--range` guard output as evidence, not a bypass.
  - Check graph.db, embeddings and search rows for references to part-1/2/3/3B ids first. Each producer re-runs its own stale-row sweep.
  - Previously awaiting Harry's sign-off; still needs it.

### CDN-0191 | C11_table_glyph: formula characters inside pipe tables
- **Root cause:**
  - The formula-glyph regex at `scripts/scan_corpus_error_classes.py:201` matches in rows where a legal formula or diagram was forced into a pipe table at ingest, e.g. gst 78-15 and 79-95.
  - Same mechanism the CDN-0193 note proved for 82-150: no reviewed ingest ever wrote these acts.
  - The line check stops at the first match (`continue` at :315), so glyph rows hide co-occurring defects: 11 midword and 3 truncated.
- **Fix:**
  - Use the gated rebuild path that already exists: `table_rebuild_staging.py` with `ingest_reingest_gate.py` (R1-R6).
  - Source PDFs: `source/{fbt-1986,taa-1953,itaa-1936,sis-1993}` and the GST PDFs (after S2).
  - Rebuild rows that are formulas as formula fences, not tables.
  - Work per act, with a rollback tag per act.
- **Blast radius:** 111 findings in 38 files. By act: fbt-1986 45, gst-1999 29, itaa-1936 18, taa-1953 16, sis-1993 3. None in itaa-1997.
- **Verification:**
  - C11_table_glyph goes to 0 for each rebuilt file.
  - `corpus_change_guard` passes with no bypass.
  - `table_rebuild_gate --selfcheck` 9/9.
  - A failing run: a rebuilt file that still emits C11_table_glyph, or a G1/R1 token-provenance reject.
- **Dependencies and risk:**
  - LARGE, and needs Harry's sign-off (Phase 1 stopped on 2026-09-16 under the >5% reject rule).
  - Do S1/S2 first so source pinning is right.
  - Fixing glyphs will surface hidden midword/truncated findings. Expect counts to rise before they fall.

### CDN-0192 | C11_table_midword: words cut across cells by fixed-column slicing
- **Root cause:** text was sliced into cells at fixed character columns, e.g. `data/itaa-1936/.../94.md:140-146` gives `| p | rimary pr | oduction income for that yea | r; and |`. The detector is `scan_corpus_error_classes.py:337-353`, and it only runs after the glyph and truncated checks have not matched.
- **Fix:**
  - Rows that carry only the midword defect: `scripts/repair_table_midword_splits.py`, which is character-preserving and checks each join against the raw text.
  - Rows that also carry truncation or glyph damage go with the CDN-0191/0193 rebuild.
- **Blast radius:** 78 findings in 31 files (itaa-1936 33, taa-1953 22, gst-1999 15, fbt-1986 8). True line incidence is 106 once masking is removed (13 hidden under rowwrap, 11 under glyph, 4 under truncated).
- **Verification:**
  - C11_table_midword goes to 0 for repaired files.
  - The existing repair script's trace to raw text for every join.
  - Run `scripts/c11_incidence_measurement.py` before and after so masked lines are counted.
- **Dependencies and risk:** see S5; this is a separate defect class from 0193. Low risk for midword-only rows.

### CDN-0193 | C11_table_truncated: table rows cut mid-clause
- **Root cause today** (the 480 figure is out of date). The remaining 78 findings in 31 files have three shapes:
  - 48: item (a) is in the table while (b)/(c) escaped as prose after it, e.g. `data/taa-1953/.../268-90.md:89-93`.
  - 14: the next row starts a new item.
  - 11: amendment-history endnote rows that wrap onto a capitalised next line (fbt 167.md, sis 381.md, gst 195-1.md). These are likely detector false positives, caused by endnotes leaking into the last section (see E-f).
  - 5: other.
- **Ingester defects named in the ticket are already fixed** (commit 037b5dae4):
  - `ingest_itaa_compilation.py:92` caps the body band with `min(BODY_Y_MAX, page.rect.height)`, and :98 plus :156-157 remove footers.
  - :219-226 only inserts the separator between alphanumeric spans, which fixes "83segment".
- **Fix:**
  - The 11 endnote rows are resolved by E-f (the endnote cutoff removes them).
  - The 48 escaped-paragraph rows need re-nesting through the gated rebuild (same path as 0191).
  - The 14 new-item rows: triage each by hand against the PDF.
- **Blast radius:** 78 findings in 31 files (taa-1953 41, itaa-1936 15, fbt-1986 11, gst-1999 7, sis-1993 4). Plus 42 header_split and 18 rowwrap, now counted as SKIP, which the rebuild will also touch.
- **Verification:**
  - C11_table_truncated goes to 0.
  - The R1 gate (no token lost against the PDF).
  - The endnote subset drops to 0 immediately after E-f, with no table edits. If it does not, the attribution is wrong.
- **Dependencies and risk:**
  - After S1/S2 and E-f. LARGE, with Harry's sign-off. An earlier cron attempt was lossy and reverted.
  - The ticket can only be closed on 0 findings, not on the reclassification.

### CDN-0196 | C5_fragment_inline: doubled "the" from column reflow
- **Root cause:**
  - (1) `itaa-1997/.../30-15.md:182` sits inside an `ingest-unclassified` fence. A printed 4-column table was dumped as monospace text, so column 2's "or the" and column 3's "the average" land on one line. It arrived with a014aefff.
  - (2) nz-master-tax-guide build-land.md:100, exemption.md:100 (twice) and tests.md:68 are multi-column tables flattened into prose.
  - C5 does not skip fences.
- **Fix:**
  - (1) Convert the fence to a pipe table through the gated rebuild (0191 path, vol02 comp 266).
  - (2) Column-aware re-extraction of those 3 guide pages from the source PDFs committed in 283c2bc3b.
  - Also make C5 report fence hits as a separate class `C5_fragment_in_unclassified`, so the real defect (an unresolved table) is named.
  - Do NOT delete the word: that would hide the table defect.
- **Blast radius:** 5 findings in 4 files.
- **Verification:** C5_fragment_inline goes to 0. A new self-test `test_c5_fragment_inline.py` plants a fenced doubled "the" and a prose doubled "the" and asserts each lands in its own class.
- **Dependencies and risk:** small. Part (2) needs a guide-specific extractor, so it can slip to a later batch without blocking the rest.

### CDN-0202 | C14_formula_structure: formula operators and fraction bars lost
- **Root cause:**
  - The printed operators and fraction bars are vector drawings, not text, so text extraction keeps the terms and drops the operators.
  - The scanner states the count is a floor (`scan_corpus_error_classes.py:487-493`).
  - The 15 en-dash false positives were already fixed in 415812ba7.
- **Fix:**
  - `scripts/apply_operator_proposal.py` reads operators off each page's drawings (OPERATORS list in the file). Apply in blocks with its existing rollback tag (:175-177).
  - Do the E-g PUA minus signs in the same pass: they are the same class of lost operator, in other acts.
- **Blast radius:** 65 findings in 50 files, all itaa-1997 (vol07 16, vol04 12, vol08 10, vol09 9, ...). C14_fraction_rule: 24 in 21 files.
- **Verification:**
  - C14_formula_structure goes to 0, and `scripts/test_c14_formula_structure.py` (8/8, phase 4) keeps pinning both directions.
  - The resolver's invariant: every added character was read off the page.
- **Dependencies and risk:** per-page work, LARGE. Comp-266 PDFs must be the pinned source (S1).

### CDN-0204 | Related-ruling titles are truncated fragments
- **Root cause (the ticket names the wrong file):**
  - `data/rulings/rulings_list.json` is never read at runtime. It is a gitignored dump from `scripts/generate_rulings_index.py`, read only by `tests/test_vectors.py:219`.
  - The served path is `_graph_rulings_for_section`, which reads `full_title or title` from `load_rulings()`.
  - At HEAD, `data_loader.py:737-756` takes the next non-empty line after the citation line as the title. ATO PDFs wrap long titles, so that line is the middle of the title.
  - The complete title already exists in the meta sidecar `title` and in `data/rulings/summaries/<stem>.json` (11,954 files). HEAD used them only when the extracted title equalled the stem or exceeded 200 characters.
- **Fix:**
  - Commit the uncommitted extractor (`_wrapped_title`, `_best_authoritative`, `_is_title_truncation`): it joins wrapped lines and prefers the authoritative title when the extracted one is a proper prefix-fragment of it.
  - Also fix the regression that extractor introduces: `backend/routes/rulings.py:78` `_tree_title` appends the description only when `full_title != title`, so 32 more rulings (364 vs 332) show a bare citation. Compare against the citation/stem instead.
  - Regenerate `rulings_list.json` with a `derived_from` header (source dir plus git sha).
- **Blast radius:**
  - HEAD: 273 of 11,952 titles flagged (219 start lowercase, 17 start with a digit or "(", 106 end on a connector word such as "of"/"to").
  - With the diff: 26 flagged, mostly ATOIDs. 282 titles change (TD 241, TR 26, GSTR 8, TA 4, other 3), and none became newly bad.
- **Verification:**
  - A test asserting that TR 2023/2, 2021/1, 2020/2, 2024/3, 2023/3 and 2021/4 have a `full_title` starting "Income tax".
  - A ruling-title detector (class `C19_ruling_title_fragment`) that fails at HEAD with 273 and passes at 26 or fewer, with the 26 listed.
  - A `_tree_title` test asserting the description is present.
- **Dependencies and risk:** Batch 0 (commit the diff). Restart :8765 after merging, or the ticket keeps reproducing.

### CDN-0205 | Related cases show the citation instead of party names
- **Root cause:**
  - At HEAD, `fastmcp_server.py:790-792` builds `_load_cases_map` from `load_cases()`, which only covers `CASE_DIR` (an external folder of 525 files).
  - The fallback at :727 is the graph label, and all 5,053 case nodes in graph.db are labelled with their citation.
  - The REST route `/api/cases/{act}/{section}` has the same gap by a different path: `data_loader.get_cases_for_section` (:446-466). The diff does not touch it.
- **Fix:**
  - Commit `load_case_name_index()` (CASE_DIR, then `scripts/cleaned/summaries`).
  - Route `get_cases_for_section` through it too.
  - Fix `short_case_name`, which returns "Taxation" for 574 AAT names of the form "X and Commissioner of Taxation (Taxation)".
  - Durable fix: write case names into the graph node labels when the graph is built, with a law-report citation crosswalk.
- **Blast radius:**
  - Named: 263 of 5,053 at HEAD, 2,047 with the diff.
  - Still unnamed: 3,006, of which about 1,900 are law-report citations ((1990) 21 ATR 1 etc.) and 1,113 are medium-neutral citations with no summary.
  - Other sources add only about 50.
- **Verification:**
  - A test that every `related.cases` entry for itaa-1997 8-1 has `title != citation` on both the MCP and REST paths. REST fails today on both servers.
  - A graph name-coverage metric in the nightly (fails if it falls below 2,047).
- **Dependencies and risk:**
  - Batch 0.
  - The index reads about 8.5k JSON files, roughly 1 s on first call, then cached.
  - The 3,006 gap is missing data, not a bug. Track it as a follow-up ticket rather than keeping 0205 open.

### CDN-0206 | Related commentary: no relevance ranking, missing `source` and `chapter_title`
- **Root cause:**
  - At HEAD, `fastmcp_server.py:~610-618` sorts by `e.weight DESC LIMIT 10`. All 112 `explained_in` edges for 8-1 have weight 1.0 (method `regex`: 73 MTG, 39 MTE), so the tie falls to node-id order and MTE ids (30xxx) come first.
  - `chapter_title` is hard-coded `""` (:668) and no `source` key is ever set.
  - The uncommitted diff alternates the two publications and fills the fields. But it breaks ties by key, so results are alphabetical by chapter: MTG ch-16 (Deductions, 47 of the 73 links) never appears, while "Illegal activities" does.
  - The diff also surfaces the mojibake from E-a (875 chapter_titles).
- **Fix:**
  - In the commentary selection in `fastmcp_server.py`, rank by the number of links from the chapter to the section (a per-chapter link count for the target), then interleave by publication.
  - Fall back to `tree.json` titles for the 193 graph slugs missing from `section_index.json`. Those 193 are the ligature-id mismatch in E-c, so fixing E-c removes most of this fallback.
  - Depends on E-a so `chapter_title` is clean.
- **Blast radius:** get_section for every section with `explained_in` edges. No data change.
- **Verification:**
  - A test for 8-1: both publications present, `source` non-empty, every `chapter_title` non-empty and free of Cyrillic, at least one MTG ch-16 row.
  - The ch-16 assertion fails with the current diff; the fields assertion fails at HEAD.
- **Dependencies and risk:** Batch 0, and E-a before release. Real relevance weights on edges would be better, but are out of scope here.

### CDN-0209 | Terms with "&" or an apostrophe are lost from the 995-1 definitions
- **Root cause** (in HEAD `scripts/build_definitions_index.py`):
  - **`&`:** the term class at :40, `[A-Za-z0-9\s'/(),%-]`, has no `&` and no left-boundary check. The match restarts after the `&`, so "R&D entity" becomes "d entity". The same gap is in CALLED :54, IN_THIS :62 and COLON :69.
  - **Apostrophe:** mostly a stale artefact. The scan body at :290 strips only `*`, so a curly `’` cuts the capture. But today's 995-1.md has 0 curly and 99 straight apostrophes: curly quotes existed only between a014aefff and db1e4cddf.
  - The on-disk `data/definitions_comprehensive.json` still has 119 curly apostrophes, so it was built from that window.
  - HEAD rebuilt on today's corpus already recovers the arm's-length terms and still loses all 5 R&D terms.
- **Fix:**
  - Keep the uncommitted diff's `&`/`’` class, `clean_scan_body` and left-boundary check.
  - Add `generator` and `source_commit` fields to the output (the file records no provenance today; schema is `{count, definitions:[{term, act, act_display, section, definition, source}]}`).
  - The served catalogue is NOT this file: it is `data/definitions_all.json` plus `data/itaa-1997/definitions.json` (`data_loader.py:192-195`), produced by `pipeline/extract_definitions.py`, and that has no `&` terms either. Apply the same `&` fix there.
- **Blast radius:**
  - 995-1: 5 R&D terms plus 15 single-letter heads ("s length", "d entity") in the on-disk file; 3 remain at HEAD on today's corpus.
  - Other acts: R&D family terms in itaa-1936 and nz.
  - The only consumer of `definitions_comprehensive.json` is `scripts/verify_graph_readiness.py:287` (a count check). The file is gitignored (`.gitignore:49`).
- **Verification:**
  - Commit the AustLII 995-1 term list (currently only at `/tmp/austlii-995-1.txt`) as `tests/fixtures/austlii_995_1_terms.txt`.
  - Fixture test: a 995-1-style paragraph with "R&D entity", "arm’s length profits" and "RBA surplus" must yield exactly those terms and nothing matching `^[a-z]\s`. It fails at HEAD ("d entity", "s length profits").
  - Recall test on 995-1: 98% or more (HEAD on today's corpus 97.7%; diff 98.1%).
- **Dependencies and risk:** the diff drops real terms (see 0210). Do not commit it as it stands.

### CDN-0210 | 995-1 term field holds prose and list fragments
- **Root cause:**
  - Nothing checks where a term starts or ends. :40 lets a capture run 80 characters across list markers and commas.
  - :307 uses `m.start()` without looking at what comes before it.
  - `is_valid_def_term` (:212-235) rejects only bare markers such as "a)" (:221). It has no check for a trailing connector word or comma, and no handling of ", in relation to X," tails.
- **Fix:** keep the diff's `starts_after_list_marker`, `repair_term` and `_CONNECTOR_TAILS`, and correct three defects in it:
  - Remove `"a"` and `"an"` from `_CONNECTOR_TAILS` (working copy :48). As it stands it drops "Zone A" (itaa-1936 s 79A).
  - Duplicates are keyed on term+act and the first one wins (:412, :446). As a result 37 scoped definitions (e.g. "exempt income, in relation to a partnership", s 90) fold into another section's entry. Key on term+act+section when a qualifier was stripped.
  - Reject captures that begin with a section number (`^\d+[a-z]?\s`): 87 corps heading captures such as "57a meaning of corporation (1)".
- **Blast radius:**
  - 995-1 list fragments 50 → 0; terms over 8 words 109 → 60 (the ones sampled are real long terms).
  - All acts: 1,162 terms dropped and 539 added. Mostly junk, but see the three defects above.
- **Verification:**
  - The 0209 fixture test extended with `(a) of a depreciating asset,` (must yield no term).
  - Guard tests: `zone a` (itaa-1936/79A) survives; "exempt income, in relation to a partnership" keeps section 90.
  - No 995-1 term matches `^\(?[0-9a-z]{1,4}\)\s`.
- **Dependencies and risk:** same batch as 0209. The `definition` text still runs past the end of each definition (mean 9.8 KB, max 433 KB, from `find_definition_end`). That is a separate defect: log it as a new ticket and do not fold it in here.

---

## Part B. Character and encoding classes (no ticket yet)

All of these share one detector function, `scan_character_classes()`, in `scripts/scan_corpus_error_classes.py`. It emits a separate class per defect, is registered in `main()` and in the shared registry (S3), and has one self-test, `scripts/test_c20_character_classes.py`, with one planted positive and one planted negative per class. It must cover section .md files, `data/*/tree.json`, `data/*/section_index.json`, rulings text/summaries, and derived case-summary text (`scripts/cleaned/summaries`). The current scanner has no detector for any of these (nothing matches FEFF, 2011, FB0x, E000-F8FF, 00AD, Cyrillic, entities or dot leaders).

### E-a | MTG "тАв" mojibake (a bullet decoded as CP866)
- **Root cause:**
  - `tax-docs.zip` (`/home/harrison/projects/ARCHIVE_cadena-knowledge-MCP/data/tax-docs.zip`, 418 entries, created on Windows with backslash paths) stores 22 filenames containing U+2022 with the UTF-8 flag (bit 0x800) set. They are the only 22 entries with that flag.
  - The unzip step ignored the flag and decoded the names as CP866.
  - `ARCHIVE.../pipeline/ingest_cch_commentary.py:249-255` then took chapter titles from those filenames (regex `Ch\s+(\d+)\s+-\s+(.+)\.pdf`), writing them into `ARCHIVE.../pipeline/output/master_tax_guide.json` (44 occurrences).
  - The in-repo producer `pipeline/build_cch_explorer.py` reads that JSON and copies `ch.get("title")` into `ch_title` (:144), which becomes the tree part title (:149) and the section_index (:207, :221).
  - `scripts/ingest_2025_docs.py:69` uses the same filename-to-title pattern for the Postgres path.
  - The mangled filenames also exist in `projects/cadena-knowledge-MCP-backup-20260217-1842`, `cadena-knowledge-MCP.bak.202603050849` and `cadena-knowledge-MCP-oauth-wip`, as well as the archive (22 files each).
- **Fix:**
  1. **Data repair** (`scripts/repair_mtg_mojibake.py`, following the `fix_page_rule_artifacts` conventions): for each string containing `[Ѐ-ӿ]`, replace it with `s.encode('cp866').decode('utf-8')`. Apply only where that round trip succeeds and the result contains no Cyrillic. Targets: `data/master-tax-guide/section_index.json` and `tree.json`.
  2. **Pipeline rule:** `build_cch_explorer.py:144` takes the chapter title from the PDF's own first-page heading or the upstream JSON's content field, never from a filename. The same rule goes in `docs/INGESTION_RULESET.md`: never derive a title from a filename.
     - Also repair `ingest_2025_docs.py:69`.
     - Leave the archive copies unchanged (not served), and record in the provenance log that they are the origin.
  3. **Detector:** `C20_non_latin_script`: any character in a non-Latin script block (Cyrillic, Greek outside math context, CJK, Arabic, Hebrew) in a Latin-script corpus field fails. OECD and NZ Māori macrons are Latin and do not fail.
- **Blast radius:** 1,370 occurrences in 897 fields across 2 files (22 distinct strings). The round trip succeeds for all 22 strings with 0 failures. Nothing in section .md files, search_index.db, graph.db or embeddings.db. These are the only Cyrillic characters in `data/`.
- **Verification:**
  - C20 fails today with 1,370 occurrences and passes at 0 after the repair.
  - Verify pass: for every changed string, re-encoding the new value back through the reverse mapping gives the HEAD value byte for byte, and the diff touches only `chapter_title` / part `title` values.
  - Self-test plants "Deductions тАв Gifts" (must fail) and "Tāwhirimātea" (must pass).
- **Dependencies and risk:**
  - Must land before 0206 ships, because the diff surfaces these titles.
  - Postgres `chapter_title` (`fastmcp_server.py:1540-1555`) was not checked; check it in the same batch.

### E-b | case_texts HTML entities
- **Root cause:**
  - `scripts/download_case_texts.py:111` saves AustLII HTML unchanged into `data/case_texts/`.
  - These files are served only as a download (`backend/routes/tax_cases.py:180-197`, linked from `TaxCaseContent.tsx:173`), so the browser renders the entities correctly. In HTML they are not a defect.
  - The real defect is in the derived text. The summary-preparation cleaners replace named entities with spaces and leave numeric ones raw: `scripts/generate_missing_summaries.py:30`, `batch_summarize_chunk.py:31`, `process_chunk.py:25`, `process_chunk_2.py:26`, `process_chunk_3.py:34`. So "R&amp;D" becomes "R D" (loss), and `&#8217;` reaches `case_summaries_fts`.
  - The one backend unescape is `fastmcp_server.py:2287` (search inside case text).
  - SearchPanel renders snippets with `dangerouslySetInnerHTML` (`SearchPanel.tsx:659`), so the display is fine, but the FTS tokens are wrong.
- **Fix:**
  - **Do not rewrite the 6,169 HTML files.** Decoding entities in HTML would corrupt `&lt;`/`&amp;` in markup and gains nothing.
  - Instead, decode at the text-derivation boundary: one helper, `html_to_text()` (`html.unescape` after tag stripping), in `backend/services/text_cleaner.py`, imported by the five scripts in place of their copy-pasted cleaners.
  - Repair in place the 50 summary rows that hold 52 raw entities, and rebuild `case_summaries_fts` with its own producer.
- **Blast radius:** raw HTML 585,838 entities in 6,169 files (unchanged, by design). Derived: 52 entities in 50 FTS rows, plus an unknown number of named entities already turned into spaces in summaries. The loss is not recoverable without re-deriving from HTML.
- **Verification:**
  - `C21_html_entity` runs on derived text only (summaries, FTS source, section/ruling markdown), not `.html`. It fails today with 52 or more.
  - Unit test: `html_to_text("R&amp;D &#8217;s")` gives "R&D ’s".
- **Dependencies and risk:** re-deriving summaries that were turned into spaces needs the LLM summary path, so do not re-summarise. Only fix entities that are still present, and log the rest as known loss.

### E-c | Typographic ligatures (U+FB00-FB06) never NFKC-normalised
- **Root cause:**
  - Nothing in the pipeline normalises with NFKC. The only NFKC call is a test comparison at `scripts/randomised_api_mcp_test.py:28`.
  - The backend hides it for the UI and `sections_fts` with a fold table (`data_loader.py:31-43`, applied at :131-140, :297-298, :1250).
  - The raw paths do not fold: MCP get_section reads the raw file (`fastmcp_server.py:1030`), embeddings read raw text (`scripts/embed_legislation.py:270`), and `insolvency_fts` is built from raw text (`search_service.py:243`). In Keays, "ﬁnancial" is a separate token, so a search for "financial" misses it.
  - Commit 1f8c551ac NFKC-renamed the MTG files and tree ids but not `section_index.json`, leaving 193 stale ligature ids. That is the 0206 fallback gap.
- **Fix:**
  - Producers: in `pipeline/build_cch_explorer.py:35-42` (`normalize_quotes`/`clean_markdown_text`) and `pipeline/parse_keays.py`, apply `unicodedata.normalize('NFKC', ...)` restricted to U+FB00-FB06. Full NFKC would also change superscripts and fractions in formulas, which is not acceptable.
  - Data repair: the same targeted mapping over MTG sections, `section_index.json` (ids and titles) and insolvency-keays chapters.
  - Then re-embed and rebuild `insolvency_fts`, each through its own producer.
- **Blast radius:**
  - MTG: 18,688 in 1,315 section files, plus 264 in 258 section_index fields (193 ids).
  - insolvency-keays: 3,107 in 21 files.
  - maps/: 11 in 5 files.
  - Keays raw text is not served; leave it.
- **Verification:**
  - `C22_ligature` fails today with 21,000 or more, passes at 0.
  - Verify: NFKC of the HEAD text equals the new text over the changed spans, and the file length changes by exactly the ligature count.
  - Test: `insolvency_search("financial")` returns the Keays rows that currently need "ﬁnancial".
- **Dependencies and risk:** changing the 193 ids changes MTG URLs. Check graph.db `explained_in` targets and add redirects if the ids differ from the tree. Mechanical and low risk.

### E-d | nz-it-2007 BOM characters; aml-ctf-2006 bold headings glued to text
- **Root cause:**
  - **BOM:** `pipeline/parse_nz_it.py:37-38` `clean_text` relies on `\s`, which does not match U+FEFF, and neither does `para_text` (:50-62). The BOMs sit between subsection brackets ("subsection (1)﻿(a)", e.g. `nz-it-2007/.../YB-4.md:25`).
  - **Stranded bold:** the brief's 1,148 / 54 does not reproduce (0 under line-only `**`, odd `**` count, `**` next to a BOM, and `****`).
  - **What aml-ctf-2006 actually has:** 176 bold headings glued onto the end of the previous paragraph in 89 files, e.g. `aml-ctf-2006/.../252.md:20` "...effect to this Act. **Penalties**", matched by regex `[.;:)] \*\*[A-Z][^*\n]{0,100}\*\*[ \t]*$`.
- **Fix:**
  - **BOM:** strip U+FEFF in `parse_nz_it.clean_text`, with a data repair (delete the character). Combine with the CDN-0173 re-parse.
  - **aml:** find the aml ingester (`scripts/ingest_aml_act.py`), confirm where the heading is joined to the paragraph, fix that, and repair the data by moving the heading onto its own line.
- **Blast radius:**
  - BOM: 1,947 in 714 files, plus 1,849 in 714 FTS rows. Plain FTS tokens are unaffected; quoted/LIKE searches and "(1)(a)" citation regexes fail.
  - aml: 176 in 89 files.
- **Verification:**
  - A `C23_invisible_char` class (U+FEFF, U+00AD, U+200B-U+200D outside tables) fails today with 1,947 or more in nz.
  - A `C24_glued_heading` class fails today with 176 in aml.
  - Verify pass for BOM: the new text equals HEAD with U+FEFF removed. For aml: the same tokens in the same order, and only newline positions changed.
- **Dependencies and risk:**
  - I did not trace the aml ingester line numbers. Establish them first; if `ingest_aml_act.py` is not the producer, find the one that is.
  - Ask whoever measured 1,148/54 for their definition. It may describe a state that has since been fixed.

### E-e | corporations-act-2001: non-breaking hyphens (U+2011)
- **Root cause:** `scripts/ingest_corps_act.py:33` `clean()` maps NBSP and hair space but not U+2011. FTS5 (porter, unicode61) treats U+2011 as a separator, so unquoted search works. Quoted phrases go through LIKE (`backend/services/search_service.py:529-545`): `"sub-fund"` returns 0 rows while `"sub‑fund"` returns 206. Browser Ctrl-F fails the same way.
- **Fix:** add `.replace('‑', '-')` in `clean()`, and re-ingest together with CDN-0124 (same run, same staging diff). For summaries and private-rulings text (90 and 68 occurrences), use the same mapping in their text cleaners.
- **Blast radius:** 3,034 in 1,014 section files, plus 256 in corps `tree.json` titles, plus 90 in case summaries and 68 in private rulings. Top words: sub‑fund 706, sub‑funds 176.
- **Verification:**
  - `C23_invisible_char` is extended with a `nonstandard_hyphen` subclass (U+2010, U+2011) and fails today with 3,034 or more.
  - Search test: quoted `"sub-fund"` returns 206 rows or more.
  - Verify: the only character changes are U+2011 to '-'.
- **Dependencies and risk:** tied to the 0124 re-ingest, which rewrites the corps corpus once.

### E-f | Dot leaders
- **Root cause.** There are two different populations:
  - **Artefact, gst/sis/fbt:** almost the whole count sits in the last section of each act: `gst-1999/.../195-1.md:2483` (one 77k-character line), `sis-1993/.../381.md:772,775`, `fbt-1986/.../167.md:809`. "Endnote 4 — Amendment history" leaked into that section and was flattened. The parsers `pipeline/parse_gst1999.py` and `pipeline/parse_fbt_sis.py` have no endnote cutoff.
    - The same leak explains the 11 endnote findings in CDN-0193 and sis 381's "endnote segmentation" abort.
    - OECD has a table of contents leaked into `oecd-mtc-2017/sections/c31-32.md:1736` (202 spaced leaders).
  - **Legitimate:**
    - itaa-1997 checklists (Div 10/11/12/13, e.g. `11-15.md:78`)
    - MTG rate and calculation layouts (31 files)
    - master-gst-guide glossary
    - NZ MTG signature blanks (30)
    - OECD fill-in blanks (17)
  - C16 covers underscores only (`scan_corpus_error_classes.py:600`, `"_" * 6 in line`).
- **Fix:**
  - Add an endnote cutoff to `parse_gst1999.py` and `parse_fbt_sis.py`: stop the last section at the "Endnotes" heading.
  - Data repair: truncate 195-1 / 381 / 167 at the endnote heading. The removed text is not law, but record it in the provenance log and diff it against the PDF endnote pages.
  - OECD: remove the table-of-contents block from c31-32.
- **Blast radius:**
  - Sections: gst-1999 804, sis-1993 762, itaa-1997 739, master-tax-guide 340, fbt-1986 315, plus others (about 3,600 in total).
  - The artefact subset is 3 files (gst/sis/fbt) plus OECD 1. itaa-1997 and MTG are legitimate, so no change.
- **Verification:**
  - `C25_dot_leader_endnote`: a run of 5 or more dots in a line longer than 2,000 characters, or following an "Endnote" heading, in a section file. It fails today on the 3 files.
  - A separate `C25_toc_leak` catches the spaced ". . . ." table-of-contents shape.
  - The legitimate checklist lines must not fire; the self-test plants `child care subsidy ...... 52-150` as a negative.
  - Re-run C11: the 11 endnote findings in 0193 must disappear.
- **Dependencies and risk:**
  - These files are also in the 0193 rebuild queue. Do this first: it is a pure truncation with a clear boundary.
  - rebuild.sh stage 5 regenerates GST sections from raw, so the cutoff must be in the parser or a rebuild will bring the text back.

### E-g | Private-use-area glyphs (Symbol font) and soft hyphens
- **Root cause:**
  - pdftotext passes Symbol-font code points through as U+F0xx. The raw readers are `pipeline/parse_gst1999.py:574`, `parse_itaa36.py:494` and `parse_fbt_sis.py:553`.
  - F02D is a minus sign, so formulas lose their operator: `gst-1999/.../131-55.md:70` "$100 + $10  $60" should read "$100 + $10 − $60", and `itaa-1936/.../51AJ.md:65` "D  A".
  - F0B7 is a bullet. F028/F029, F05B/F05D and F0E9-FA are large bracket pieces. F0AD/F0AF (OECD) are arrows.
  - Regulatory guides use Wingdings (F09F bullet 1,716, F0D8 arrowhead 144).
  - Soft hyphens:
    - ATO IDs, from `pipeline/fetch_ato_id_content_v2.py:83` `get_text()`, used as a list bullet.
    - CRs, from `scripts/fetch_new_rulings.py:26` `normalize`, standing in for a real hyphen: `CR_2023_59.txt:1` "non­employee" renders as "nonemployee".
- **Fix:**
  - A shared `SYMBOL_PUA_MAP` in one module, imported by the three parsers.
  - Data repair maps F02D to U+2212, F0B7 to U+2022, bracket pieces to ASCII brackets, arrows to U+2191/U+2193.
  - Unmapped code points are left in place and reported.
  - Wingdings map for regulatory guides.
  - Soft hyphen: a real hyphen where it sits between letters, removal where it acts as a list bullet (AID_2003_*), in both fetchers and in the data.
- **Blast radius:**
  - PUA in sections: gst-1999 15, fbt-1986 12, itaa-1936 19, oecd 4.
  - PUA elsewhere: regulatory-guides 1,919 in 63 files; case_texts 212 numeric entities in 16 files (in HTML, so leave them).
  - Soft hyphens: 22 in 3 summary files plus 13 in 5 ruling .txt files; FTS `rulings_fts` 13 in 5 rows and `private_rulings_fts` 1,292 in 280 rows.
- **Verification:**
  - `C26_pua_glyph` fails today with 46 or more in sections and 1,919 in regulatory guides.
  - The soft-hyphen subclass of C23 fails today with 35 or more.
  - Verify: every changed character was PUA or U+00AD, and the mapping table is the only source.
  - For the minus signs, check each against the PDF page with the `apply_operator_proposal` read-off-the-page method, since a missing minus changes the law's arithmetic.
- **Dependencies and risk:** the F02D minus signs are real meaning loss, so group them with the CDN-0202 operator pass. The rest is cosmetic.

---

## Part C. Source-side items from the 2026-09-25 nightly
(Report: `/home/harrison/.hermes/cron/output/597343d6e44f/2026-09-25_00-09-24.md:620-642`)

### S1 | rebuild.sh compilation guards disagree with the vendored sources
- **Root cause:**
  - `rebuild.sh:72` expects `itaa-1997 266`, `itaa-1936 192`, `gst-1999 96`, `taa-1953 225`, checked by `scripts/check_source_compilation.py` (reads "Compilation No." from the first 3 pages).
  - What is actually there:

  | Act | Guard | source/ PDFs (internal) | Corpus frontmatter |
  |---|---|---|---|
  | itaa-1997 | 266 | **263** (C2026C00122 VOL01-12) | 266 (4,649 files) |
  | itaa-1936 | 192 | 191 | 191 (993) |
  | taa-1953 | 225 | 222 | 222 (1,243) |
  | gst-1999 | 96 | no `source/gst-1999/`; `source/gst-1999-vol1,2.pdf` = 96 | 96 (827) |

  - The nightly treats all three as one stale-source problem. It is two problems: itaa-1997 has the wrong PDFs, while for 1936 and TAA the guard values are wrong.
- **Rule out the script's own remedy for itaa-1997.** `rebuild.sh:87-90`, option (b), says "change the --compilation-no ... and accept that the corpus becomes that older compilation".
  - For itaa-1997 that would rebuild from comp-263 PDFs and from `data/itaa-1997/raw/*.txt` (comp 263, 2026-05-17), replacing the real comp-266 corpus (commit 091945207).
  - At least 26 sections that exist only in comp 266 would be deleted (e.g. 40-291A, Div 119, 112-155..185).
  - Option (b) is rejected for itaa-1997.
- **Fix:**
  - **itaa-1997:** vendor the 12 comp-266 volumes (`/home/harrison/legislation-explorer-staging/data/itaa-1997/raw/comp266/vol01-12.pdf`, internal "Compilation No. 266", C2026C00324, 26 MB) into `source/itaa-1997/`, replacing the 263 set.
    - Delete `data/itaa-1997/raw/*.txt`, because `rebuild.sh:48` skips extraction when `raw/` is newer than the PDFs.
    - Keep 266.
  - **itaa-1936 and taa-1953:** repoint the expectations and stamps (`rebuild.sh:72`, :106/:114/:128/:136) to 191 and 222, date 2026-04-01. This matches the source and the corpus, so nothing is downgraded.
  - Also add a guard of the guard: compare the expected number with the corpus frontmatter `compilation_no` and abort if the source is older than the corpus. Remedy (b) could then never downgrade silently.
- **Blast radius:** `rebuild.sh` (about 6 lines); `source/itaa-1997/` (24 files swapped, +26 MB in plain git: no LFS, `source/` is tracked); `data/itaa-1997/raw/` deleted (regenerable). No section files change.
- **Verification:**
  - Run `check_source_compilation.py` for each act: all exit 0.
  - A new `scripts/test_rebuild_guards.py` parses `rebuild.sh:72` and asserts, for each act, expected == PDF-internal == corpus `compilation_no`. It fails today on 3 acts.
  - `rebuild.sh` itself is not run as a test: stage 1 writes `data/*/raw` before the guard at 1b (:35-60), so it has no write-free mode. Moving guard 1b before stage 1 is part of this fix.
- **Dependencies and risk:**
  - Vendoring binaries grows the repo by 26 MB. Acceptable, and it matches how the other 30 source files are kept.
  - Do this before any gated table or formula rebuild, which reads from `source/`.

### S2 | No `source/gst-1999/`, so the GST guard never runs
- **Root cause:**
  - `rebuild.sh:75`, `if [ -d "$act_dir" ]`, silently skips a missing directory.
  - `extract_pdfs "$SOURCE/gst-1999"` (:59) finds nothing.
  - Stage 5 (:124-129) then deletes `data/gst-1999/sections` and re-parses the old `raw/vol0{1,2}.txt`. It is harmless today only because that raw is also comp 96.
- **Fix:**
  - `git mv source/gst-1999-vol1.pdf source/gst-1999-vol2.pdf source/gst-1999/` (internal comp 96, C2026C00081, 430 and 306 pages).
  - In `rebuild.sh:75`, a missing `source/<act>/` for a guarded act must be a failure, not a skip.
  - Repoint any reference to the old root paths (the calibrator mentioned in the CDN-0193 note).
- **Blast radius:** 2 files moved, about 3 lines of `rebuild.sh`, plus reference updates.
- **Verification:** `test_rebuild_guards.py` (S1) asserts that every guarded act has a `source/<act>/` directory. It fails today on gst-1999.
- **Dependencies and risk:** grep for `gst-1999-vol` before the move.

### S3 | C15/C16 are not in the nightly; their self-tests are not picked up
- **Root cause:**
  - `scan_drawing_characters` (`scan_corpus_error_classes.py:605`, C15) and `scan_page_rule_artifacts` (:581, C16) are called from `main()` (:765-766), but not from the nightly's hard-coded 15-name list (`scripts/randomised_api_mcp_test.py:342-347`).
  - That list in turn includes `scan_body_fragments`, which `main()` does not call.
  - Phase 4's glob is `test_c[0-9]*_*.py` (`final_data_audit.py:83`). C15's test is `scripts/test_scan_drawing_chars.py`, which does not match.
  - C16 has no detector self-test: `scripts/test_page_rule_fix.py` tests the fixer.
- **Fix:**
  - Add one list, `DETECTORS = [...]`, in `scan_corpus_error_classes.py`. Both `main()` and `randomised_api_mcp_test.py` iterate over it, so the two registries cannot drift.
  - Rename the C15 test (`git mv`) to `scripts/test_c15_drawing_chars.py`.
  - Add `scripts/test_c16_page_rule.py`: a planted `______` line in prose must fire; the same line in a fence must not.
- **Blast radius:** 3 files plus 1 new test. No data change.
- **Verification:**
  - Phase 4 collects test_c15 and test_c16.
  - Add an assertion in `final_data_audit.py` that every class prefix emitted by `main()` has a `test_c<N>_*.py`. It fails today for C15/C16 and every class without a test, so land it as a report that lists them, then make it fail once the list is empty.
- **Dependencies and risk:**
  - Registering C15 immediately surfaces 22 findings in 11 itaa-1997 files. Open a ticket for them rather than letting the nightly go red without one.
  - This batch comes first, because every Part B class depends on the shared registry.

### S4 | 23 master-tax-examples files are duplicates of registered sections
- **Root cause:**
  - Both files of each pair arrived in commit 19068816f (2026-06-11): two slug-length generations in one regeneration.
  - Each of the 23 unregistered files has a filename cut at 80-81 characters, with the longer registered twin alongside, e.g. `1-290-...-personal-services-busine.md` vs `...-business.md`.
  - After collapsing whitespace and bullets, the bodies are identical. The only other differences are line breaks and the `section:` slug.
  - No script in the repo detects this; it was an ad-hoc check by the nightly agent.
- **Fix:**
  - Delete the 23 unregistered twins (`git rm`), after confirming no reference in backend, scripts, graph.db, embeddings or search rows. None was found in backend, scripts or the act JSON; graph and embeddings still need checking.
  - Detector `C27_unregistered_section`: any section file not referenced by its act's `tree.json`/`section_index.json`. This is a general orphan check, so it catches the next slug-generation drift in any act.
- **Blast radius:** 23 files (333 → 310). Embedding and search rows for those ids are deleted by their own producers.
- **Verification:**
  - C27 fails today with 23 or more (and may report orphans in other acts: triage those, do not bulk-delete).
  - Verify: each deleted file's normalised body equals its registered twin's body.
- **Dependencies and risk:** a deletion through the corpus guard, with the guard's `--json` output as evidence. Low risk.

### S5 | Are C11_table_midword and C11_table_truncated one class reported twice?
- **Finding:** they are two distinct defects.
  - The line check stops at the first match, so the same line cannot be reported under both: glyph `continue` at `scan_corpus_error_classes.py:315`, truncated `continue` at :334, midword tested only after (:337-353).
  - Measured on today's output: 11 of 31 files are shared; 0 shared lines; 6 pairs within ±1 line; 4 lines genuinely carry both signatures once first-match-wins is removed (94.md:146, 121AS.md:57/203/205).
  - 20 files have only midword and 20 only truncated.
  - Truncated is mostly the escaped-paragraph shape (48/78), not a word cut.
  - The nightly's own example (9-26.md:37 vs :51) is two rows 14 lines apart.
  - The identical "78/31" for both is a coincidence.
- **Fix:**
  - No merge.
  - Change the per-line check to report every signature (no `continue`), with a dedup key of (file, line, class), so masked lines become visible. This matches the earlier "counts are a floor" notes.
  - Record the verdict in both tickets.
- **Blast radius:** the detector only. Counts rise to true incidence: midword about 106 lines, truncated about +4.
- **Verification:**
  - Extend a self-test (`test_c11_table_coherence.py`, new) so a planted row carrying a glyph, a trailing connector and a mid-word split emits all three classes. It fails today (emits one).
  - `corpus_change_guard` imports these detectors (`corpus_change_guard.py:37-41`), so re-run `test_corpus_change_guard.py` 6/6.
- **Dependencies and risk:** the guard blocks any C11 finding present in NEW but not OLD. Because OLD is re-scanned with the same code, unmasking alone will not block commits. Confirm this with the retro-run.

### X1 | The pre-commit hook skips untracked files unless a section is staged (found while checking conventions)
- **Root cause:** `scripts/pre_commit_corpus_guard.sh:11` exits 0 unless `git diff --cached` names a section file. So the untracked scan in `corpus_change_guard.py:194` never runs on a commit that stages only code, and a bad untracked file can sit until an unrelated corpus commit.
- **Fix:** widen the early-exit test to also check `git ls-files --others --exclude-standard -- data | grep -Eq sections/.*\.md`.
- **Blast radius:** 1 line.
- **Verification:** add a case to `scripts/test_corpus_change_guard.py`: a bad-shape untracked section file plus a staged `.py` file, and the commit must be refused. It fails today.
- **Dependencies and risk:** it could block commits on existing untracked scratch under `data/`. Run `git ls-files --others data` first.

### X2 | `fix_page_rule_artifacts.py` provenance lives in /tmp and there is no rollback tag
- **Root cause:** `scripts/fix_page_rule_artifacts.py:29` writes `/tmp/cdn203-apply.json`, and the script creates no tag. This is the template every Part B repair would copy.
- **Fix:** move the log to `docs/provenance/<ticket>-<stamp>.json` and add the tag step from `apply_operator_proposal.py:175-177`.
- **Blast radius:** 1 script.
- **Verification:** a smoke test runs `--apply` on a temporary copy of a fixture, then asserts the tag and log exist. It fails today.
- **Dependencies and risk:** do it before writing the Part B repair scripts, so they inherit the fix.

---

## Recommended execution order (batches grouped by shared fix site)

| Batch | Items | Shared fix site | Data change? |
|---|---|---|---|
| **0 Settle the working tree** | 0204, 0205, 0206 (partial), 0209/0210 (partial), quotes.json, `_abs` | Split the uncommitted diff into: (a) routes/loader for 0204/0205, (b) commentary, (c) definitions builder, (d) `get_case` URLs, (e) quotes.json. Commit only (a) and (d) now, with tests. Restart :8765. | No |
| **1 Detector plumbing** | S3, S5, X1, X2, plus the C17-C27 detectors, all failing first | `scan_corpus_error_classes.py` (shared `DETECTORS` list), `final_data_audit.py`, `randomised_api_mcp_test.py`, the guard hook, the repair-script template | No |
| **2 API and UI** | 0204 (`_tree_title` fix), 0205 (REST route, `short_case_name`), 0206 (link-count ranking), 0099 | `fastmcp_server.py`, `data_loader.py`, `routes/rulings.py`, `routes/cases.py`, `SearchPanel.tsx` | No (0206 goes live only after batch 3) |
| **3 MTG commentary data** | E-a, E-c (MTG + section_index ids + Keays), S4 | `pipeline/build_cch_explorer.py` (:35-42, :144), MTG `section_index.json`/`tree.json`, master-tax-examples | Yes: about 1,370 + 21,000 characters, 23 files deleted |
| **4 Definitions** | 0209, 0210, plus the `&` fix in served definitions | `scripts/build_definitions_index.py`, `pipeline/extract_definitions.py`, AustLII fixture | Derived only |
| **5 Source pinning** | S1, S2 | `rebuild.sh`, `source/itaa-1997/`, `source/gst-1999/` | Source only |
| **6 Corporations Act re-ingest** | 0124, E-e | `scripts/ingest_corps_act.py` (`clean()` :33, fallback :286-303) | Yes: about 403 chapeaus, 3,034 + 256 characters |
| **7 NZ re-parse** | 0173, E-d (BOM) | `pipeline/parse_nz_it.py` (:37-62, :193) | Yes: 445 files deleted, 1,947 characters |
| **8 Parser boundaries and glyph maps** | E-f (endnote cutoff, OECD table of contents), E-g (PUA map, soft hyphens), E-d (aml glued headings), E-b (summary cleaner) | `parse_gst1999.py`, `parse_fbt_sis.py`, `parse_itaa36.py`, fetchers, `text_cleaner.py` | Yes: 3 endnote truncations, 46 PUA, 35 soft hyphens, 176 headings, 52 entities |
| **9 Gated table and formula rebuild (LARGE, needs Harry's sign-off)** | 0191, 0192, 0193, 0196, 0202, the E-g minus-sign check | `table_rebuild_staging.py`, `ingest_reingest_gate.py`, `apply_operator_proposal.py` | Yes: per act, per block |

Ordering reasons:
- Batch 1 goes before any data batch, so every repair has a detector that fails first.
- Batch 3 goes before 0206 is released.
- Batch 5 goes before batch 9, which reads `source/`.
- E-f (batch 8) goes before batch 9, because it removes the 11 endnote findings in 0193 and sis 381's abort cause.
- Batches 6 and 7 each rewrite one act once, taking two tickets with them.

## Pure user-visible wins (no data rewrite, or a trivially reversible one)
- **0204:** full ruling titles in related rulings (273 → 26 bad titles corpus-wide).
- **0205:** party names for related cases (263 → 2,047 named).
- **0206:** MTG commentary actually appears, with source and chapter.
- **0099:** live suggestions (the endpoint already exists).
- **E-a:** "тАв" disappears from 22 MTG sidebar chapter titles and from API output.
- **E-e:** quoted Corporations Act searches for hyphenated words start matching (0 → 206 for "sub-fund").
- **E-c:** Keays search for "financial" etc. starts matching.

## Real data loss vs cosmetic

**Real loss or wrong law text:**
- **0124:** about 403 Corporations Act sections lost their opening words. The text is recoverable from finreg.db.
- **0191 / 0192 / 0193 / 0196:** table content scrambled, cut or left as a dump (111 / 78 / 78 / 5 findings).
- **0202:** 65 formulas missing drawn operators or fraction bars.
- **E-g F02D:** minus signs missing in gst 131-55, itaa-1936 51AJ and others. The arithmetic reads wrong.
- **0173:** 445 non-law sections served as nz-it-2007 law. This is contamination: the consolidated sections are intact.
- **E-f:** endnote and table-of-contents text served inside the last section of gst, sis, fbt and OECD. Contamination.
- **0209 / 0210:** R&D terms missing, and about 8% of 995-1 entries unusable in the derived definitions index. The served catalogue lacks R&D too.
- **E-b (derived):** named entities turned into spaces in case summaries. Not recoverable without re-summarising.
- **0205 remainder:** 3,006 graph cases with no name anywhere. This is missing data, not corruption.

**Cosmetic or reversible (no meaning lost; affects display or search only):**
- E-a mojibake (fully reversible)
- E-c ligatures
- E-d BOM
- E-e U+2011
- E-g bullets, brackets and arrows
- Soft hyphens (list-bullet form; the in-word form in 2 CRs mis-renders a word but loses no text)
- aml glued headings
- S4 duplicate files
- Legitimate dot leaders (no action)
- Raw case_texts HTML entities (not a defect; no action)

## Not established
- The brief's "stranded bold" counts (1,148 nz, 54 aml) could not be reproduced. The aml glued-heading class (176) is my substitute, and needs confirming.
- The brief's 1,039,390 case_texts entity count could not be reproduced (measured 585,838).
- The aml ingester and its line numbers (E-d) were not traced.
- The Postgres `chapter_title` column (`fastmcp_server.py:1540-1555`) was not checked for the E-a mojibake.
- graph.db and embeddings references to the 23 master-tax-examples twins and the 445 NZ part ids were not checked.
- The 995-1 AustLII parse gives 1,404 terms, not the ticket's 1,484, so the ticket's "38 of 47" split could not be reproduced.
